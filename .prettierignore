node_modules
ios
android
dist
.expo

# Written by scripts/ffmpeg/build.sh: vendored FFmpeg sources and build output.
.ffmpeg-build

# Generated by `npm run test:playback -- --update-baselines`, written with
# JSON.stringify(…, 2). Prettier collapses short arrays, so every baseline run
# left format:check red until someone remembered to reformat them.
test/playback/baselines

# Generated by Expo, regenerated on every prebuild.
expo-env.d.ts

# Generated by `npm run licenses`. The generator owns the formatting, so that
# `npm run licenses:check` can compare its output to the committed file byte for
# byte; reformatting here would make that check fail on every run.
constants/bundled-licenses.ts
